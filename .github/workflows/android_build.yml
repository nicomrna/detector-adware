name: Build Android APK

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v5
      with:
        java-version: '17'
        distribution: 'temurin'

    # Verifica si existe gradlew; si no está, lo instala
    - name: Ensure Gradle Wrapper
      run: |
        if [ ! -f ./gradlew ]; then
          echo "Gradlew no encontrado, instalando..."
          sudo apt-get update && sudo apt-get install -y gradle
          gradle wrapper
        fi
        chmod +x gradlew

    # Compilación ignorando pruebas y análisis estático (lint)
    - name: Build Debug APK
      run: ./gradlew assembleDebug --stacktrace -x test -x lint

    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: AdwareShield-APK
        path: app/build/outputs/apk/debug/app-debug.apk
